@inherits RazorBlade.PlainTextTemplate<GeneratorFiles>;
@{
    var done = Model.Done();
    var notDone = Model.NotDone();
}
---
sidebar_position: 10
title: Patterns
---

# Patterns With Examples from .NET  :  @done.Length

@foreach (var item in done)
{
    <text>
        ## [@item.Title](/docs/patterns/@item.Title)



    </text>
}

@if (notDone.Length > 0)
{
    <text>
        # Patterns to be added : @notDone.Length

        @foreach (var item in notDone)
        {
            <text>
                ## @item.Title
            </text>
        }
    </text>
}